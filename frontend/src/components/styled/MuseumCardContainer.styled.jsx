import styled from "styled-components";

const StyledMuseumCardContainer = styled.div`
  display: flex;
  flex-direction: column;
  padding: 30px 0;
  background-color: rgba(34, 47, 62, 1);

  button {
    margin: 20px auto 0 auto;
  }
`;

export default StyledMuseumCardContainer;
